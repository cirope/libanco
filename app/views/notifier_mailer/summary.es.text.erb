Hola <%= @user %>,
Éstos son los eventos para mañana <%= l(Date.tomorrow, format: :minimal) %>:
================================================================================

<% @schedules.each do |schedule| %>
  <%= l(schedule.scheduled_at, format: :time) %> | <%= schedule.description %>
  <% if schedule.customer %>
    <%= Customer.model_name.human(count: 1) %>: <%= schedule.customer %>
    <%= Customer.human_attribute_name :phone %>: <%= schedule.customer.phone %>
    <%= Customer.human_attribute_name :cellphone %>: <%= schedule.customer.cellphone %>
    <%= Customer.human_attribute_name :address %>: <%= schedule.customer.address %>
  <% end %>
<% end %>

Podés organizar tus eventos en Libanco desde
<%= link_to 'acá', "#{schedules_url(subdomain: @user.account.subdomain)}/#{l(Date.tomorrow, format: :calendar)}" %>
