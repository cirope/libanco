Para el <%= l(@reminder.scheduled_at, format: :long) %>:
================================================================================

<%= @reminder.description %>

<% if @reminder.schedulable
  model = @reminder.schedulable.class %>
  <%= @reminder.schedulable.model_name.human(count: 1) %>: <%= @reminder.schedulable %>
  <%= model.human_attribute_name 'phone' %>: <%= @reminder.phone %>
  <%= model.human_attribute_name 'cellphone' %>: <%= @reminder.cellphone %>
  <%= model.human_attribute_name 'address' %>: <%= @reminder.full_address %>
<% end %>

Podés organizar tus eventos en Libanco desde
<%= link_to 'acá', "#{schedules_url(subdomain: @reminder.account.subdomain)}/#{l(Date.today, format: :calendar)}" %>
