<%= render 'cashes/cash' %>

<%= render template: 'cashes/flows/show', locals: { '@cash': @cash } %>

<hr />

<% if @cash.opened? %>
  <%= link_to fa_icon('arrow-circle-left', text: t('cashes.flows')),
    cash_flows_path(@cash), class: 'btn btn-warning' %>
<% end %>
