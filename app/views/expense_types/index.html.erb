<% if @expense_types.blank? %>
  <%= render 'shared/empty_index', message: t('.empty_html') %>
<% else %>
  <div class="table-responsive">
    <table class="table table-striped table-condensed">
      <thead>
        <tr>
          <th><%= ExpenseType.human_attribute_name 'created_at' %></th>
          <th><%= ExpenseType.human_attribute_name 'name' %></th>
          <th></th>
        </tr>
      </thead>

      <tbody>
        <% @expense_types.each do |expense_type| %>
          <tr>
            <td class="col-md-2"><%= l expense_type.created_at %></td>
            <td><%= expense_type.name %></td>
            <td><%= link_to_edit [:edit, expense_type] %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% end %>

<hr />

<%= paginate @expense_types %>
<%= link_to_new new_expense_type_path, class: 'btn btn-primary' %>
